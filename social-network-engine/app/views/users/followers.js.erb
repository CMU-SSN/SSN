<% if !@user.followers.nil? && @user.followers.length > 0 %>
$(".userData").html("<% @user.followers.each do |follower| %><%= escape_javascript(render "partials/user_item", :user => follower, :path => @path, :feedItemId => @feedItemId) %><% end %>");
<% else %>
	$(".userData").html("");
<% end %>