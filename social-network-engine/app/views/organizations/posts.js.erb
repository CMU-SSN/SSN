<% if !@posts.nil? && @posts.length > 0%>
$(".organizationData").html("<% @posts.each do |post| %><%= escape_javascript(render "partials/feed_item", :post => post, :path => @path) %><% end %>");
<% else %>
	$(".userData").html("");
<% end %>